import { Highliteielement } from './highliteielement';

describe('Highliteielement', () => {
  it('should create an instance', () => {
    const directive = new Highliteielement();
    expect(directive).toBeTruthy();
  });
});
